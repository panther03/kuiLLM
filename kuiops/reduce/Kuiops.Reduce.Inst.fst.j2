module {{ module }}
#lang-pulse

// JIT-GENERATED instantiation of the exact polymorphic HReduce kernel.
// Generated from kuiops/reduce/Kuiops.Reduce.Inst.fst.j2

open Kuiper
open Kuiper.Tensor
open Kuiper.Tensor.Layout.Alg { l1_forward }
open Kuiper.Functions
open Kuiops.HReducePoly.Exact

{% if admit_associativity %}
assume val {{ assoc_name }} : is_associative ({{ reduce }})
{% endif %}

let {{ name }}
  (lena : sz { lena <= max_blocks * max_threads /\ lena > 0 })
  (a : array1 {{ et }} (l1_forward lena) { is_global a })
  (#va : chest1 {{ et }} lena)
  =
  reduce #({{ et }}) ({{ reduce }}) ({{ pre_map }}) {{ threads }}sz lena
    #(l1_forward lena) a #va
