module {{ module }}
#lang-pulse

// JIT-GENERATED instantiation of the verified Kuiper last-axis mean kernel.
// DO NOT EDIT. Generated from kuiops/mean/Kuiops.Mean.Inst.fst.j2
//
// One GPU block per output row cooperatively tree-reduces that row's
// {{ length }} cells (Kuiper.Kernel.HReduce.Block) and divides by the baked
// length; see Kuiops.Mean.{fst,fsti}. Parameterised only by the element type
// and the reduced length, so it is reused across all ranks/batch sizes.

open Kuiper
open Kuiops.Mean

let {{ name }} = mean_inst #{{ et }} {{ length }}L
